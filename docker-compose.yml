version: "3.9"
services:
  multiagent:
    build: .
    container_name: multiagent-app
    ports:
      - "8000:8000"   # FastAPI
      - "8501:8501"   # Streamlit
    environment:
      - BACKEND_URL=http://127.0.0.1:8000
